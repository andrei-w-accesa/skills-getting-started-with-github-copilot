[pytest]
pythonpath = .
markers =
	activities_get: tests for the GET /activities endpoint
	activities_signup: tests for the POST /activities/{activity_name}/signup endpoint
	activities_unregister: tests for the DELETE /activities/{activity_name}/participants endpoint
